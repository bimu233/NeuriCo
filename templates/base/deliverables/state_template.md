# Session Title
_A short and distinctive 5-10 word descriptive title for this research session. Super info dense, no filler. Example: "FineTuning vs RAG PubMedQA Phase3 Experiments"_

# Current State
_What phase is actively running right now? What tasks are pending and not yet complete? What is the immediate next step when resuming? Always update this section LAST so it reflects the most recent snapshot._

# Research Specification
_What is the research hypothesis? What datasets and evaluation metrics are being used? Any design decisions or constraints (compute budget, time limit, memory). Include the work_dir path so it survives context compaction._

# Files and Resources
_What are the important files in this workspace? What datasets were downloaded and where are they stored? Key scripts and what they do. Include absolute paths — the agent will need these to resume after a context flush._

# Workflow
_What commands are run and in what order to reproduce the experiments? How to interpret their output? Example: `python run_experiment.py --config config.yaml` produces `results/metrics.json`._

# Errors & Corrections
_Errors encountered and exactly how they were fixed. What approaches failed and must NOT be tried again? What did the researcher correct? Be specific: include error messages, file paths, line numbers where relevant._

# Experiment Design
_Baselines used and why. Evaluation metrics and how they are computed. Experimental configuration (hyperparameters, seeds, splits). How components fit together. This section replaces "Codebase and System Documentation" for research contexts._

# Learnings
_What has worked well? What has not? What domain insights emerged? Do not duplicate items already in other sections. Focus on non-obvious findings that would help replicate or extend this work._

# Experiment Results
_Actual numerical results: accuracy, F1, latency, cost, or other metrics. Include the complete table or output the researcher requested — exact numbers, not summaries. This section replaces "Key results" for research contexts._

# Worklog
_Step by step, what was attempted and done across all phases? One terse line per step. Include phase transitions: "Phase 1 complete → Phase 2 started". This is the audit trail._
